generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Pokemon {
  id     Int      @id @default(autoincrement())
  name   String   @unique
  sprite String
  types  String[]
}